---
name: policy-explain
description: 解读政策、法规、规划、标准、办法、白皮书等文件，说清它对解读者所在行业与岗位的实际影响。当用户要求解读或读懂某份政策文件、评估新规对本行业本岗位的影响、提炼文件中的任务与机会、把文件做成解读型知识网页时使用。首次使用会先确认使用者的行业与岗位，再以五维度框架输出可操作结论。触发词：政策解读、文件解读、法规解读、规划解读、标准解读、白皮书解读、五维分析、政策分析、上级文件解读、读懂政策、政策影响、新规影响
---

# Policy Explain — 文件五维解读

## 概述

本技能解决一件事：**说清一份文件对某个具体行业、某个具体岗位到底意味着什么。**

把任意领域的政策、法规、规划、标准、办法、白皮书，按解读者的**行业与岗位**，用五维度框架转成可操作的结论。生成 Wiki 型知识网页只是可选的呈现方式，不是技能目的。不要为了把网页做得漂亮而稀释解读质量，也不需要等用户开口要网页才去做解读。

本技能不预设任何行业。解读视角由使用者本人在首次使用时声明并长期保存。

## 核心原则

解读的价值来自视角。同一份文件，医院科主任、企业合规经理、工程技术人员读出来的结论完全不同。因此每份解读都必须锚定一个明确的身份：

**行业 + 单位类型 + 岗位 + 主要关切**

通用化不等于中性化。把视角抹平成"人人都适用"的表述，输出的就是文件摘要，而不是解读。身份必须具体。

## 工作流

### 第 0 步：确认解读身份

**1. 定位身份档案**

档案是用户级个人配置，不绑定任何特定宿主的主目录。按下列优先级解析，取第一个可写位置：

| 优先级 | 位置 | 说明 |
|---|---|---|
| 1 | `$POLICY_EXPLAIN_HOME/profile.json` | 环境变量已设置时使用。便于隔离多套身份或做测试 |
| 2 | 宿主的标准用户配置目录 | Windows：`%APPDATA%\policy-explain\profile.json`；Linux／macOS：`${XDG_CONFIG_HOME:-$HOME/.config}/policy-explain/profile.json` |
| 3 | 技能目录内 `.profile.json` | 前两处都不可写时才回退 |

解析纪律：

- **不得硬编码宿主主目录**。同一份技能可能在 WorkBuddy、Codex、Claude Code 等不同宿主里运行，档案位置应由运行时环境决定。
- 路径中的用户名取当前登录用户，不要写死。Windows 上无法展开 `%APPDATA%` 时，用 `<用户主目录>\AppData\Roaming\policy-explain\profile.json`。
- 目录不存在则创建；不可写则回落下一优先级。
- 若落在技能目录内（第 3 处），必须确认该文件已被版本库忽略，**绝不能把身份信息提交到仓库**。本技能仓库已在 `.gitignore` 中忽略 `.profile.json`。
- **三档全部不可写**（罕见，通常出现在只读挂载或权限受限环境）：不再尝试落盘，改为**会话内记忆**。在对话中记下身份并在后续步骤沿用，同时告知用户"本次身份仅在本会话有效，下次需重新声明"。

**2. 档案不存在 → 引导用户填写**

四项信息都要落到，可一次性提问：

| 字段 | 说明 | 示例 |
|---|---|---|
| industry | 所在或关注的行业/领域 | 智能制造、基层医疗、文旅、建筑施工、金融、农业、教育培训 |
| org | 单位类型 | 民营制造企业、三甲医院、文旅集团、机关事业单位、行业协会 |
| role | 岗位/角色 | 合规经理、科室主任、项目经理、技术负责人、一线技术员 |
| concerns | 主要关切（可多项） | 资质与合规风险、绩效考核指标、项目与资金申报、个人技能升级 |

引导语可直接使用：

> 先确认一下解读视角，这样结论才能落到你身上。四项即可：①所在或关注的行业 ②单位类型 ③岗位或角色 ④最关心什么（申报／考核／合规／技能／资源）。一句话回答也行。

**追问规则**：若回答过于笼统（例如只答"教育""医疗"），追问一次，给出 2-3 个更具体的候选让用户选，不要替他假设。行业与岗位是后续全部结论的锚点，此处省一步，后面全是空话。

**用户明确拒绝声明身份时**：不要卡死在第一步。按"通用读者"降级推进，并在输出**首行**标注：

> 本解读未锚定具体岗位。第五维仅列文件明示的申报与激励口径，不含个性化建议。

降级只在用户明确拒绝时启用。用户只是答得不全（例如给了行业没给岗位），仍按上面的追问规则补问，不走降级。

**3. 写入档案并回显确认**

下面只是格式示范，实际写入用户本人声明的身份，不得沿用示例内容：

```json
{
  "default": "智能制造·合规经理",
  "profiles": [
    {
      "name": "智能制造·合规经理",
      "industry": "智能制造与装备制造",
      "org": "民营制造企业",
      "role": "合规与质量经理",
      "concerns": ["体系认证与企业资质", "安全生产合规风险", "技改项目申报"],
      "created": "2026-01-01"
    }
  ]
}
```

**4. 档案已存在 → 不重复询问**

读出默认身份，一句话确认后直接进入解读。若默认身份与本次文件明显不符（例如身份是医疗，文件是旅游规划），问一句"本次按哪个身份解读？"，按次覆盖，不修改档案。

**5. 支持多身份**

同一人可保存多个身份（如"部门负责人""技术骨干""行业协会成员"），解读结论本就不同。用户可随时要求新增或切换。

> **✅ 检查点**：把最终选定的**档案路径**与**身份一句话**一起回显给用户，得到确认后再进入第 1 步。身份是后续全部结论的锚点，这一步不能默认通过。

### 第 1 步：输入文件

用户提供：文件原文或链接、目标受众（默认取档案身份）、是否需要网页或 Word。

**原文获取与降级**

| 情况 | 处理 |
|---|---|
| 有原文或可访问的官方链接 | 直接按其解读 |
| 只有二手转述、解读文章、新闻稿 | 先联网核实官方原文，核实成功则按原文解读 |
| 联网仍拿不到原文 | 明确告知"以下基于二手来源，量化指标未经原文核实"，并在每个引用要点后标注来源。**不得把二手数字当作确定事实陈述** |

**超长文件**（正文超过约 3 万字或 50 页）：不要通读硬解。先按目录或一级标题切块，用与本次身份相关的关键词定位命中段落，只精读命中块及其上下文。输出时给出章节或条款坐标，便于用户回溯核对。

### 第 2 步：五维度解读

下表是骨架。**「核心追问」「关键词」「受众之问」三列都要依据第 0 步声明的身份现场改写**，示例列只示范改写方向，不是预设库，不可直接套用。

| 维度 | 核心追问（骨架） | 关键词来源 | 受众之问（骨架） | 改写示例 |
|---|---|---|---|---|
| 1 背景与风向 | 出台方为什么在这个时间点发这份文件 | 取文件中的时机词与战略词 | 这是不是我所在领域的大趋势，原地不动会不会落后 | 教育：转型窗口／医疗：控费信号／文旅：消费提振 |
| 2 核心任务 | 文件要求干什么，有哪些硬指标 | 取量化指标与任务清单 | 我单位接下来会被推动什么、检查什么 | 通用性最强，基本不改 |
| 3 相关条款定位 | 文件中直接点名本行业、本岗位的条款在哪 | 取点名本行业的段落关键词 | 我的岗位与哪些条款直接对应 | 医疗：分级诊疗·DRG／文旅：研学·海洋旅游／制造：技改·智能制造 |
| 4 要求与变化 | 对流程、工具、考核、能力提出了什么新要求 | 取动词型要求 | 我明天的工作方式要改什么 | 企业：合规·留痕·审计／教学：数字化·过程性评价 |
| 5 机遇与行动 | 藏着哪些项目、资金、资质、荣誉或技能红利 | 取激励性表述 | 我能申报什么，手头工作能否换个更对文件的表述 | 个人：申报·评优·认证／机构：试点·资质·资金 |

每个维度输出五项：

- **核心追问**：引导思考的关键问题
- **关键词**：3-5 个，从文件原文提取，不做领域臆测
- **受众之问**：标签写成本次身份的口吻（如"科室主任的思考""合规经理的思考"），内容落到该身份的切身关切
- **解读**：结合原文的详细分析，给出具体行动指向
- **结论框**：一句总结性判断（💡格式）

**表格化**：凡存在"原文要点 → 行动指向"对照关系的，一律用表格呈现，不要写成连续段落。

维度 5 需区分**个人视角**与**机构视角**，两者可申报的东西不同。

> **✅ 检查点**：五维解读完成后，先交付文本解读并确认结论可用，再询问是否需要网页或 Word。**不要跳过确认直接生成网页。**

### 第 3 步：可选输出 —— 生成 Wiki 网页

**必读**：先完整读取 `references/README.md`，它列有全部 52 个占位符的含义与可复用组件写法。不读它会写错占位符。

**取骨架**：读取 `references/wiki-template.html`，替换全部 `{{占位符}}` 与区块标记。骨架中留有 1 个完整示例区块（`#d1`），维度 2-5 按其结构复制，仅改 `id` 与内容。

**参照成品**：不确定版式时读 `references/example-education.html`，它是唯一的完整样例。

**不要加载** `example-education.html` 来抄内容。它是某个特定行业的样例，只用于确认版式；其中的行业用语必须全部替换。

生成要求：

1. **成品中不得残留骨架示例内容，也不得残留上一份解读的行业用语。**
2. 页面包含：顶部导航、侧边目录（滚动高亮）、五个维度区块、原文与行动对照表、结论框、受众行动清单、响应式布局。
3. 版式遵循骨架（结构、层级、交互不变）。配色允许按受众气质微调，但**不得使用青＋深色、紫蓝渐变这一类 AI 感配色**。

成品保存到用户指定目录，文件名建议 `<文件简称>_政策解读Wiki.html`。

**自由度说明**：骨架只约束版式，不约束判断。解读内容按本次身份自由组织；占位符清单是必读的，但每个占位符填什么，由第 2 步的解读结论决定。

## 输出格式

默认交付**文本解读**。网页与 Word 是按需附加的呈现方式，第 2 步的解读质量才是本技能的核心产出。

| 形态 | 做法 |
|---|---|
| 文本解读 | 直接输出五维度解读，格式清晰，可直接复制 |
| Wiki 网页 | 按第 3 步生成单文件 HTML，保存到指定目录 |
| DOCX 文档 | 如需 Word，优先调用当前宿主已有的文档生成能力（如 WorkBuddy 的 `tencent-docx` 插件、其他宿主的同类技能）；宿主没有对应能力时，再用 pandoc 或 python-docx 兜底 |

## 反模式清单

| 反模式 | 为什么 |
|---|---|
| 用中性表述替代身份视角 | 输出会退化成文件摘要，本技能的价值归零 |
| 直接套用五维表的示例列 | 示例只标方向，套用等于没做身份改写 |
| 关键词凭领域常识臆造 | 结论无法回溯原文，审阅时会被推翻 |
| 成品残留上一份解读的行业词 | 复用模板最常见的事故，读者一眼看出是套壳 |
| 用二手转述代替原文解读 | 新闻稿常曲解量化指标，结论会错得自信 |
| 为把网页做得漂亮而牺牲解读 | 网页是可选呈现，解读质量才是产出 |
| 把身份信息提交到版本库 | 公开仓库会泄露用户的岗位信息 |
| 跳过第 0 步直接解读 | 没有身份锚点，五个维度全会写空 |
